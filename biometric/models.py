from django.db import models


# BiometricTemplate model lives in the employees app (OneToOne with Employee).
# This app holds the service/listener layer only.
# Placeholder to keep the app importable.
